include!("stdlib");

read("../inputs/01.txt")
	>> split("\n\n")  
	// >> map(print)
	>> map(\>> split("\n") >> map(int) >> sum)
	>> sort
	>> rev
	:>> elfs;

elfs[0] >> print;
elfs >> take(3) >> sum >> print;

