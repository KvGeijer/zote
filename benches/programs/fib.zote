fn fib(n) -> {
	// Just testing out returns, and comments
	if n < 0 {
		return 0;
	};
	
	if n == 0
		1
	else if n == 1 
		1
	else {
		var x = 2;
		var y;

		x = fib(n-2) + (y = fib(n-1))
	}
};


var i = -1;
var serie = [];
while (i = i + 1) < 20	
	fib(i) >> push(serie);

print(serie)
